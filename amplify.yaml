version: 1
backend:
  phases:
    preBuild:
      commands:    
    build:
      commands:
frontend:
  phases:
    preBuild:
      commands:
        - cd yAppWeb
        - npx pnpm install
    build:
      commands:
        - npx pnpm run build
  artifacts:
    baseDirectory: yAppWeb/dist
    files:
      - '**/*'
  cache:
    paths:
      - yAppWeb/node_modules/**/*